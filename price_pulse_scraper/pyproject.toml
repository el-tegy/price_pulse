[tool.poetry]
name = "price_pulse_scraper"
version = "0.1.0"
description = "Giving you the heartbeat of pricing across sites ��🔍"
authors = ["el-tegy <elisee.tegue@groupe-esigelec.org>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
scrapy = "^2.11.0"


[tool.poetry.group.dev.dependencies]
pylint = "^3.0.1"
black = "^23.10.0"
isort = "^5.12.0"
pre-commit = "^3.5.0"
pytest = "^7.4.3"
pytest-cov = "^4.1.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.pytest.ini_options]
addopts = [
    "--import-mode=importlib",
]
pythonpath = "price_pulse_scraper"