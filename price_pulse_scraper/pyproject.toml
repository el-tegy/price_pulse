[tool.poetry]
name = "price-pulse"
version = "0.1.0"
description = "Giving you the heartbeat of pricing across sites ��🔍"
authors = ["el-tegy <elisee.tegue@groupe-esigelec.org>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
